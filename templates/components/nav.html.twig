<nav class="navbar sticky-top navbar-expand-lg bg-nav align-items-start pb-0">
    <div class="container-fluid ps-0 pe-0">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarExpanded" aria-controls="navbarExpanded" aria-expanded="false" aria-label="Toggle navigation">
            <i class="bi bi-three-dots text-white fs-1"></i>
        </button>
        <div class="collapse navbar-collapse nav-dimensions flex-column align-items-start" id="navbarExpanded">
            <span class="content-text ps-2"><i class="bi bi-rocket-takeoff me-2"></i>Dry Dock</span>
            <hr class="text-white w-100">
            <a class="nav-link {{ view=='shipyard'?'active':'' }}" href="{{ path('shipyardView') }}" data-turbo-frame="tf-view"><i class="bi bi-boxes me-2"></i>Shipyard</a>
            <a class="nav-link {{ view=='view-parts'?'active':'' }}" href="{{ path('partsView') }}" data-turbo-frame="tf-view"><i class="bi bi-search me-2"></i>View Parts</a>
            <a class="nav-link {{ view=='create-part'?'active':'' }}" href="{{ path('createPartView') }}" data-turbo-frame="tf-view"><i class="bi bi-gear me-2"></i>Create Part</a>
            <a class="nav-link {{ view=='build-ship'?'active':'' }}" href="{{ path('buildShipView') }}" data-turbo-frame="tf-view"><i class="bi bi-wrench me-2"></i>Build Ship</a>
            <a class="nav-link logout" href="{{ path('logout') }}" data-turbo="false"><i class="bi bi-box-arrow-left me-2"></i>Logout</a>
        </div>
    </div>
</nav>